# Copy to /etc/jackson-wakeup.env (or another path referenced by EnvironmentFile)
# and fill in your secrets.

OPENAI_API_KEY=

# Frameo sync:
# - Windows (drive letter): FRAMEO_DIR=E:\\DCIM
# - Windows (Shell/MTP path): FRAMEO_DIR=This PC\\Frame\\Internal storage\\DCIM
# - Raspberry Pi/Linux PTP (recommended): FRAMEO_DIR=gphoto2:auto
# - Raspberry Pi/Linux PTP (explicit): FRAMEO_DIR=gphoto2:/store_00010001/DCIM
FRAMEO_DIR=

# Optional web search fallback (SerpAPI/Bing). If unset, web search may fail.
WEB_SEARCH_API_KEY=

# Optional: enable Tailscale SSH by running `sudo tailscale up --ssh` (no env vars needed).
