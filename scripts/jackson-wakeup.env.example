# Copy to /etc/jackson-wakeup.env (or another path referenced by EnvironmentFile)
# and fill in your secrets.

OPENAI_API_KEY=

# Frameo sync (Raspberry Pi/Linux): set this to the mounted DCIM folder.
# Example (depends on your system/mount):
# FRAMEO_DIR=/media/pi/FRAMEO/DCIM
FRAMEO_DIR=

# Optional web search fallback (SerpAPI/Bing). If unset, web search may fail.
WEB_SEARCH_API_KEY=

# Optional: enable Tailscale SSH by running `sudo tailscale up --ssh` (no env vars needed).
